// PARAM: --enable ana.int.interval --set ana.activated[+] apron
#include <stdlib.h>
#include <goblint.h>

int main()
{
    unsigned int len = rand();
    len = len % 10;

    for (int i = 0; i -1 < len ; i++)
    {
        int tmp = i;
        // __goblint_check( i < len );
    }
}
